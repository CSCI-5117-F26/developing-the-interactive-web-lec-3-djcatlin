[project]
name = "lec3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "djcatlin", email = "deancatlin03@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "halo>=0.0.31",
]

[project.scripts]
lec3 = "lec3:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
